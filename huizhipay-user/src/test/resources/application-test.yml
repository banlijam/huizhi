# 导入测试环境敏感配置文件（包含密码等）
# 请复制 application-test-secrets.yml 并填入实际值
spring:
  config:
    import: classpath:application-test-secrets.yml
  application:
    name: huizhipay-user-test
  mail:
    host: smtp.qiye.aliyun.com
    port: 465
    properties.mail.smtp:
      auth: true
      ssl.enable: true

server:
  port: 8081

app:
  frontend:
    url: http://localhost:8080