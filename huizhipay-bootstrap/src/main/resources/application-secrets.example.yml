# ========================================
# 敏感配置文件模板 - 复制为 application-secrets.yml 并填入实际值
# Sensitive config template - Copy to application-secrets.yml and fill in values
# ========================================

spring:
  datasource:
    username: your_database_username
    password: your_database_password
  mail:
    username: your_mail_username
    password: your_mail_password

jwt:
  secret: your_jwt_secret_key_at_least_32_characters_long
