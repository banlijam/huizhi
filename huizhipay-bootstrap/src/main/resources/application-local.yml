server:
  port: ${SERVER_PORT:14319}

spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://127.0.0.1:15432/huizhipay_local}
    username: ${SPRING_DATASOURCE_USERNAME:huizhipay}
    password: ${SPRING_DATASOURCE_PASSWORD:}
  mail:
    host: ${SPRING_MAIL_HOST:localhost}
    port: ${SPRING_MAIL_PORT:2525}
    username: ${SPRING_MAIL_USERNAME:local}
    password: ${SPRING_MAIL_PASSWORD:local}

jwt:
  secret: ${JWT_SECRET:local_dummy_jwt_secret_key_20260826_minimum_32_chars}

app:
  frontend:
    url: ${APP_FRONTEND_URL:http://127.0.0.1:14318}

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
