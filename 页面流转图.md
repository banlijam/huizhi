# 页面流转图

> 来源：[[pay支付项目/pm/三大页面流转和开发详细需求，以及原型图]]
> 配套：[[pay支付项目/pm/页面清单]]
> 更新时间：2026-08-09

## 0. 读图说明

- `圆角矩形` = 页面/动作
- `菱形` = 决策点（分支）
- `带箭头实线` = 流转方向
- 图中只画**用户可见的页面流转**；底层的链上分账、路由引擎等属于系统后台，用户不可见，单独在「跨端数据流图」说明。

---

## 1. 买家端 Checkout

### 1.1 HPP 托管支付页流转（买家走支付链接）

```mermaid
flowchart TD
    A["商户生成支付链接<br/>(发送给买家)"] --> B["买家点击链接"]
    B --> C["HPP 托管支付页加载<br/>金额 + 信任徽章 + 法币显示"]
    C --> D{"选择支付方式"}
    D -->|"银行卡"| E["输入卡号 / 有效期 / CVV"]
    D -->|"本地钱包"| F["确认金额与币种"]
    E --> G["点击支付"]
    F --> G
    G --> H{"触发 3DS2 强验证?"}
    H -->|"需要"| I["3DS2 验证弹窗 Modal<br/>银行验证码输入"]
    I -->|"验证通过"| J["系统确认支付结果"]
    I -->|"验证失败 / 超时"| K["支付失败页<br/>失败原因 + 重试按钮"]
    H -->|"不需要"| J
    J -->|"支付成功"| L["支付成功页<br/>显示结果 + 返回商户"]
    J -->|"支付失败"| K
    K -->|"重试"| C
    K -->|"放弃"| M["返回商户网站 / 关闭"]
    L --> N["系统回调商户 Webhook"]
    N --> O["跳转回商户网站<br/>(return_url)"]
```

**读图要点（开发会问的问题）：**
- 3DS2 是**决策点**：什么条件触发强验证？→ 后续在风控需求里定（如金额阈值、风险评分）。
- 失败页 → 重试 → 回到 HPP 是**循环**，要防重复扣款（幂等），这是支付的核心。
- 成功后才触发 Webhook 回调，再跳转 return_url；**回调失败怎么办**（重试策略）在开发者端需求里定。

### 1.2 Embedded Widget 流转（买家在商户网站内完成支付）

```mermaid
flowchart TD
    A["买家在商户网站<br/>点击支付按钮"] --> B["Embedded Widget 展开<br/>内嵌表单，无跳转"]
    B --> C{"选择支付方式"}
    C -->|"银行卡"| D["输入卡号 / 有效期 / CVV"]
    C -->|"本地钱包"| E["确认金额与币种"]
    D --> F["点击支付<br/>(可能触发 3DS2 弹窗)"]
    E --> F
    F --> G{"支付结果"}
    G -->|"成功"| H["内嵌结果区: 支付成功<br/>(不跳转，留在商户页)"]
    G -->|"失败"| I["内嵌结果区: 失败 + 重试"]
    I -->|"重试"| B
```

---

## 2. 商户端 Merchant Dashboard

### 2.1 入驻（Onboarding & KYB）与登录流转

```mermaid
flowchart TD
    A["访问商户后台"] --> B{"已有账号?"}
    B -->|"否"| C["入驻 / KYB 表单<br/>(多步骤进度条)"]
    C --> D["步骤1: 营业执照<br/>(上传 / 填写)"]
    D --> E["步骤2: 法人信息"]
    E --> F["步骤3: 结算诉求<br/>(Crypto 或 法币)"]
    F --> G["钱包绑定<br/>Connect Settlement Wallet"]
    G --> H["提交审核"]
    H --> I{"审核结果"}
    I -->|"驳回"| C
    I -->|"通过"| J["登录页"]
    B -->|"是"| J
    J --> K["控制台首页 今日大盘"]
```

**读图要点：**
- 审核**驳回回到表单**，要支持草稿保存（填了一半能存）。
- 钱包绑定放在 KYB 提交前，是结算依赖，属于 P0。

### 2.2 登录后页面导航

```mermaid
flowchart TD
    A["控制台首页 今日大盘<br/>交易笔数/转化率/流水 + 清算倒计时"] --> B["分账账本 Ledger<br/>7:93 分账明细"]
    A --> C["风控与路由设置<br/>(规则开关)"]
    A --> D["钱包管理<br/>(查看 / 更换结算地址)"]
    A --> E["订单列表"]
    B -->|"点击某笔订单"| F["订单详情<br/>(金额拆解 $100 → $7 / $93)"]
    C -->|"打开 Strict Anti-Fraud Mode"| G["确认提示<br/>拦截阈值调高 / 强制 3DS2"]
```

---

## 3. 开发者端 Developer Portal

```mermaid
flowchart TD
    A["访问开发者门户"] --> B["登录页"]
    B --> C["控制台首页/概览<br/>API 状态 / 环境标识"]
    C --> D["API 密钥管理"]
    C --> E["沙盒模式设置"]
    C --> F["Webhook 监听中心"]
    C --> G["接口文档"]
    D --> D1["生成 Test Key / Live Key"]
    D1 --> D2["密钥只显示一次<br/>(复制后消失)"]
    E --> E1["打开沙盒开关<br/>使用测试卡 4242 4242..."]
    E1 --> H["在沙盒环境模拟完整回调"]
    H --> F
    H --> I["联调通过<br/>切换 Live Key 上线"]
    F --> F1["回调日志列表"]
    F1 --> F2["日志详情<br/>(成功 / 失败 / 重试次数)"]
```

**读图要点：**
- 沙盒 → 联调 → 切 Live Key 是一条**主线**，体现"开发者从测试到上线的完整路径"。
- 密钥"只显示一次"是安全关键点，原型图里要画清楚（显示一次 + 强制复制 + 之后只能轮换）。

---

## 4. 跨端数据流图（一笔订单的旅程）

一笔订单在三个端不是"页面跳转"，而是**同一个业务在不同角色眼中的三个视图**：

```mermaid
flowchart LR
    subgraph M["商户端"]
        M1["创建订单<br/>生成支付链接"]
        M2["Ledger 分账账本"]
        M3["清算倒计时"]
    end
    subgraph B["买家端"]
        B1["HPP / Widget 支付"]
        B2["成功页 / 失败页"]
    end
    subgraph S["系统后台 (用户不可见)"]
        S1["路由引擎 + 3DS2 验证"]
        S2["链上智能合约分账<br/>7% 服务费 / 93% 商户"]
        S3["状态同步<br/>订单标记 Settled"]
    end
    subgraph D["开发者端"]
        D1["Webhook 回调日志"]
    end
    M1 --> B1
    B1 --> S1
    S1 --> S2
    S2 --> S3
    S3 --> B2
    S3 --> D1
    S3 --> M2
    M2 --> M3
```

**读图要点：**
- 买家端只看到"法币支付"（Fiat-to-Fiat Illusion），看不到 USDT/链上分账。
- 一笔订单会同时在三个端出现：买家端是"支付结果"，商户端是"分账记录 + 清算倒计时"，开发者端是"一条 Webhook 回调日志"。
- 这张图是后面三份 PRD 需求能对齐的锚点。

---

## 5. 待确认问题（画图时暴露出来的）

1. 3DS2 强验证的**触发条件**由谁定？金额阈值 / 风控评分？
2. 支付失败后的**重试**是否限制次数？超过后怎么办？
3. HPP 与 Widget 的**成功页**是否共用一套模板？
4. 商户 KYB 提交后**审核人**是谁？（人工 / 自动）
5. Webhook 回调失败的**重试策略**（次数、间隔、死信处理）？

这些先记录，后续在 PRD 阶段逐一定掉。
